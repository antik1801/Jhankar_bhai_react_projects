import React from 'react';

const MainContainer = (props) => {
    const {datas} = props;
    return (
        <div className='card lg:grid-cols-8'>
            
        </div>
    );
};

export default MainContainer;