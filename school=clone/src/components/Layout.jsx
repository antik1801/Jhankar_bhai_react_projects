import React from 'react';
import Header from './Header';

const Layout = () => {
    return (
        <div>
            {/* Header */}
            <Header></Header>
            {/* Main section */}
            {/* Footer */}
        </div>
    );
};

export default Layout;