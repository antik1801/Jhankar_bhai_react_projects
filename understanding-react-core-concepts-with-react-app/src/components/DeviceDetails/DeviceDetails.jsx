import React from 'react';

const DeviceDetails = (props) => {
    return (
        <div>
            <h3>Information about the phone: Price={props.price}</h3>
        </div>
    );
};

export default DeviceDetails;